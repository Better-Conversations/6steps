<footer class="bg-white border-t border-slate-200 mt-auto">
  <div class="container mx-auto px-4 sm:px-6 lg:px-8 py-6">
    <div class="flex flex-col sm:flex-row justify-between items-center text-sm text-slate-500">
      <div class="flex flex-wrap justify-center sm:justify-start gap-x-6 gap-y-2 mb-4 sm:mb-0">
        <%= link_to "About", about_path, class: "hover:text-slate-700" %>
        <%= link_to "The Approach", approach_path, class: "hover:text-slate-700" %>
        <%= link_to "Privacy", privacy_path, class: "hover:text-slate-700" %>
        <%= link_to "Terms", terms_path, class: "hover:text-slate-700" %>
        <% if user_signed_in? %>
          <%= link_to "Support", crisis_resources_path, class: "hover:text-slate-700" %>
        <% end %>
      </div>
      <p class="text-slate-400">&copy; <%= Date.current.year %> Six Steps</p>
    </div>
  </div>
</footer>
