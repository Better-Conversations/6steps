<!DOCTYPE html>
<html lang="en" class="h-full">
  <head>
    <title><%= content_for(:title) || "Admin - Six Steps" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="icon" href="/favicon.ico" sizes="any">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= stylesheet_link_tag "tailwind", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <% if ENV['UMAMI_WEBSITE_ID'].present? %>
      <script defer src="https://umami.betterconversations.foundation/script.js" data-website-id="<%= ENV['UMAMI_WEBSITE_ID'] %>"></script>
    <% end %>
  </head>

  <body class="h-full bg-slate-100">
    <div class="min-h-full">
      <!-- Admin navigation -->
      <nav class="bg-slate-800">
        <div class="container mx-auto px-4 sm:px-6 lg:px-8">
          <div class="flex h-16 items-center justify-between">
            <div class="flex items-center">
              <div class="flex-shrink-0">
                <%= link_to admin_dashboard_path, class: "text-white font-semibold text-lg" do %>
                  Six Steps <span class="text-slate-400 font-normal">Admin</span>
                <% end %>
              </div>
              <div class="ml-10 flex items-baseline space-x-4">
                <%= link_to "Dashboard", admin_dashboard_path, class: "px-3 py-2 rounded-md text-sm font-medium #{current_page?(admin_dashboard_path) ? 'bg-slate-900 text-white' : 'text-slate-300 hover:bg-slate-700 hover:text-white'}" %>
                <%= link_to "Session Reviews", admin_session_reviews_path, class: "px-3 py-2 rounded-md text-sm font-medium #{current_page?(admin_session_reviews_path) ? 'bg-slate-900 text-white' : 'text-slate-300 hover:bg-slate-700 hover:text-white'}" %>
                <%= link_to "Safety Metrics", admin_safety_metrics_path, class: "px-3 py-2 rounded-md text-sm font-medium #{current_page?(admin_safety_metrics_path) ? 'bg-slate-900 text-white' : 'text-slate-300 hover:bg-slate-700 hover:text-white'}" %>
                <% if current_user.role_admin? %>
                  <%= link_to "Invites", admin_invites_path, class: "px-3 py-2 rounded-md text-sm font-medium #{request.path.start_with?('/admin/invites') ? 'bg-slate-900 text-white' : 'text-slate-300 hover:bg-slate-700 hover:text-white'}" %>
                <% end %>
              </div>
            </div>
            <div class="flex items-center space-x-4">
              <span class="text-slate-400 text-sm"><%= current_user.email %></span>
              <%= link_to "Exit Admin", dashboard_path, class: "text-slate-300 hover:text-white text-sm" %>
            </div>
          </div>
        </div>
      </nav>

      <main class="py-10">
        <div class="container mx-auto px-4 sm:px-6 lg:px-8">
          <%= render "shared/flash" %>
          <%= yield %>
        </div>
      </main>
    </div>
  </body>
</html>
